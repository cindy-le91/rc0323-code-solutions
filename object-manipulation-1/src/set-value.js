/* exported setValue */
// define a function named 'setValue' that takes three arguments, 'object', 'key', and 'value'
// assigns a value to the objects's key

function setValue(object, key, value) {
  object[key] = value;
}
