/* exported getValue */
// define function named 'getValue' that takes two arguments, 'object' and 'key'
// return the value of the key for the object

function getValue(object, key) {
  return object[key];
}
