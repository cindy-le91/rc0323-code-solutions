/* exported take */
// define a function named 'take' that takes two arguments, 'array' and 'count'
// use slice method to return 'count' elements from original array into new array
function take(array, count) {
  return array.slice(0, count);

}
