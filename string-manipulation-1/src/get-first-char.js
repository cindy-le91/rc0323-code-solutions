/* exported getFirstChar */
// define a function named 'getFirstChar' that takes one argument 'string'
// get first character of each string and return

function getFirstChar(string) {
  return string[0];
}
