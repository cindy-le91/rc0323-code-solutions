/* exported getLastChar */

// define a new function named 'getLastChar' that takes one argument 'string'
// get last character of each string and return
function getLastChar(string) {
  return string[string.length - 1];
}
