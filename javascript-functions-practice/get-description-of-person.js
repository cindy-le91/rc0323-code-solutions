/* exported getDescriptionOfPerson */
function getDescriptionOfPerson(person) {
  const getDescriptionOfPerson = person.name + ' is a ' + person.occupation + ' from ' + person.birthPlace + '.';
  return getDescriptionOfPerson;
}
